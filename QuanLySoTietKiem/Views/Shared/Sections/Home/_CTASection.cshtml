<!-- CTA Section -->
<section class="py-5">
    <div class="container">
        <div class="cta-section p-5">
            <div class="row align-items-center">
                <div class="col-lg-8 text-center text-lg-start">
                    <h2 class="mb-3 fw-bold">Start saving today</h2>
                    <p class="lead mb-0">Create a free account and start your financial journey</p>
                </div>
                <div class="col-lg-4 text-center text-lg-end mt-4 mt-lg-0">
                    <div class="d-flex gap-3 justify-content-center justify-content-lg-end">
                        <a href="@Url.Action("Login", "Account")" class="btn btn-primary btn-custom">Login</a>
                            <a href="@Url.Action("Register", "Account")" class="btn btn-success btn-custom">Register</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>